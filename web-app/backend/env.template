# Backend Environment Variables Template
# Copy this file to .env and adjust values as needed

# Application Settings
DEBUG=True
PORT=5002

# Service URLs (Docker Compose service names)
AUDIO_SERVER_URL=http://whisper_service:8000
VLLM_SERVER_URL=http://vllM_service:8000
WHISPER_SERVICE_URL=http://whisper_service:8000
REDIS_URL=redis://redis:6379/0

# Security Settings
JWT_SECRET=your_secure_jwt_secret_key_here_change_this_in_production
JWT_ALGORITHM=HS256

# NER Keywords (deprecated - use ner.env file instead)
# NER_ENCRYPTION_KEY=your_ner_encryption_key_32_chars_minimum_change_this

# Model Settings
# VLLM_MODEL=Qwen/Qwen3-0.6B  # COMMENTED OUT: Old vLLM model configuration

# NEW: Gemini API Configuration
GEMINI_API_KEY=your_gemini_api_key_here_get_from_google_ai_studio

# vLLM Service Configuration (for Docker) - COMMENTED OUT: No longer using vLLM
# VLLM_GPU_MEMORY_UTILIZATION=0.7
# VLLM_MAX_MODEL_LEN=2048
# VLLM_TENSOR_PARALLEL_SIZE=1

# CORS Settings
CORS_ORIGINS=["*"]
CORS_ALLOW_CREDENTIALS=true
CORS_ALLOW_METHODS=["*"]
CORS_ALLOW_HEADERS=["*"]

# NOTE: For NER keywords, create a separate 'ner.env' file using 'ner.env.template'
# This keeps sensitive keyword data separate and secure